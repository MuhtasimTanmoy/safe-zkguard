# Deployment
# ---- ZK / RISC Zero ----
# Recommended: use the Router so your app stays compatible with future verifier versions.
RISC0_VERIFIER=<RISC0_VERIFIER> # Fixed in mainnet
RISC0_IMAGE_ID=<RISC0_IMAGE_ID> # Update each time you recompile the zkVM code

# --- Owner / Signer ---
NICK_SEED=<NICK_SEED> # Fixed for testing; use a secure random value in production

# ---- DAO Policy Data (example values; replace with your own) ----
POLICY_HASH=<POLICY_HASH> # Update if the policy changes
GROUPS_HASH=<GROUPS_HASH> # Update if the groups change
ALLOW_HASH=<ALLOW_HASH> # Update if the allowlist changes

# ---- Safe (Ethereum mainnet, v1.4.1) ----
SAFE_SINGLETON=<SAFE_SINGLETON> # Fixed in mainnet
SAFE_PROXY_FACTORY=<SAFE_PROXY_FACTORY> # Fixed in mainnet
SAFE_FALLBACK_HANDLER=<SAFE_FALLBACK_HANDLER> # Fixed in mainnet

# ---- Deployment params ----
SAFE_SALT_NONCE=0         # choose a nonce; changing it changes the Safe address

# Interaction
# Addresses
SAFE_ADDR=<SAFE_ADDR>
ZKGUARD_MODULE=<ZKGUARD_MODULE>